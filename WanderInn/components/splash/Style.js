import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: "center",
        justifyContent: "center",
    },

    LogoImg: {
        position: 'absolute',
        height: 200,
        width: 200,
        resizeMode: 'contain',
        textAlign: 'center',
        bottom: "0",
    },
});